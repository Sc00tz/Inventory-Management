services:
  app:
    build: .
    environment:
      - PORT=3001
      - DB_PATH=/data/inventory.db
    command: sh -c "node server.js & npm run dev:client"
    ports:
      - "4000:4000"
    volumes:
      - .:/app
      - /app/node_modules
      - inventory_data:/data

volumes:
  inventory_data:
