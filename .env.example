# Copy to .env and fill in. docker-compose reads this file automatically.

# Optional. A free API key from https://upcdatabase.org adds general-merchandise
# barcode coverage (electronics, tools, household goods) that the free
# Open Food/Beauty/Products Facts databases don't carry. Leave blank to skip it —
# food and cosmetics lookups still work without a key.
UPCDATABASE_API_KEY=
